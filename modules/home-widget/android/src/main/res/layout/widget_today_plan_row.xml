<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
  android:id="@+id/row_root"
  android:layout_width="match_parent"
  android:layout_height="wrap_content"
  android:orientation="horizontal"
  android:gravity="center_vertical"
  android:paddingTop="5dp"
  android:paddingBottom="5dp">

  <!-- RemoteViews only permits a fixed set of widget classes; a plain View is
       rejected at inflation time, so the colour bar is an ImageView. -->
  <ImageView
    android:id="@+id/row_color"
    android:layout_width="3dp"
    android:layout_height="18dp"
    android:contentDescription="@null"
    android:layout_marginEnd="8dp" />

  <TextView
    android:id="@+id/row_time"
    android:layout_width="42dp"
    android:layout_height="wrap_content"
    android:textColor="#8A8A94"
    android:textSize="11sp"
    android:fontFamily="monospace" />

  <TextView
    android:id="@+id/row_title"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_weight="1"
    android:textColor="#FFFFFF"
    android:textSize="12sp"
    android:textStyle="bold"
    android:maxLines="1"
    android:ellipsize="end"
    android:layout_marginStart="4dp"
    android:layout_marginEnd="6dp" />

  <TextView
    android:id="@+id/row_target"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:textSize="11sp" />
</LinearLayout>
