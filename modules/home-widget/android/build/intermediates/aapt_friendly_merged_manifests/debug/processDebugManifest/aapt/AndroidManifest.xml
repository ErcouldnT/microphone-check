<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="expo.modules.homewidget" >

    <uses-sdk android:minSdkVersion="24" />

    <application>

        <!-- Home screen widget showing what is still planned for today. -->
        <receiver
            android:name="expo.modules.homewidget.TodayPlanWidgetProvider"
            android:exported="false" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/today_plan_widget_info" />
        </receiver>

        <!--
         Backs the widget's scrolling list. The system binds this from the
         launcher's process, so it must be exported behind BIND_REMOTEVIEWS.
        -->
        <service
            android:name="expo.modules.homewidget.TodayPlanRemoteViewsService"
            android:exported="false"
            android:permission="android.permission.BIND_REMOTEVIEWS" />
    </application>

</manifest>